# STATUS (TaskManager)

_Авто-обновление цикла: 2026-02-09 18:50 MSK_

## Текущее состояние
- UI: `/taskManager/` (логин через Telegram Login Widget)
- API: `/taskManager/api/health/` (ok)

## Что сделано (за цикл)
- FE (main): проекты + доска + drawer + **Drag&Drop** + отображение `closed_at` + базовые проверки/чеки (см. коммиты `741e92f`, `7ebbc5f`).

## В работе
- BE: модели/миграции + DRF API для Project/Task + контракт для DnD (смена статуса) + фиксация `closed_at` при переводе в Done/Today.
  - В рабочем дереве сейчас есть незакоммиченный файл: `backend/api/serializers.py` (похоже, начата работа по сериализаторам).
- OPS: после мерджа/коммитов BE — пересборка/рестарт compose и смоук через nginx.

## Риски / блокеры
- FE/BE контракт: названия статусов + эндпоинт смены статуса (и возможный ordering) нужно зафиксировать, иначе DnD придётся переделывать.
- Секреты: `.env` должен оставаться только локально (в gitignore), не коммитить.

## Следующие шаги
1) TeamLead: уточнить/зафиксировать BE задачи и контракт под FE DnD; обновить/создать `tasks/BE-*.md` при необходимости.
2) BE: довести модели+API+тесты, commit+push.
3) OPS: `docker compose up -d --build` и smoke-check через nginx.
4) PM: финально обновить STATUS/SPRINT и дать владельцу краткий отчёт.
