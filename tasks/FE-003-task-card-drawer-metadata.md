# FE-003: Карточка задачи — создание/редактирование (drawer) + бейджи/время закрытия

## Контекст
Спринт требует:
- задачи можно создавать (обязателен `title`),
- редактировать в drawer,
- показывать badge 🤖 если `source == "clawdbot"`,
- показывать время закрытия (коротко: дата+время) в правом нижнем углу, **только** если задача в Done/Today.

## Acceptance Criteria («Готово»)
- [ ] В колонке есть действие «Добавить задачу» → создаёт задачу через API (минимум: `title`).
- [ ] Клик по карточке открывает drawer редактирования:
  - [ ] редактируемые поля минимум: `title`, `description` (если есть), возможно `source` read-only.
  - [ ] кнопка сохранения вызывает API PATCH/PUT.
- [ ] На карточке:
  - [ ] если `source == "clawdbot"` → отображается badge 🤖.
  - [ ] если статус Done или Today и поле `closed_at`/`done_at` заполнено → показывается время закрытия (локальный формат, без секунд).
  - [ ] если статус не Done/Today → время не показывается.

## Статус по `STATUS.md`
- [x] FE реализовано (drawer + closed_at + source badge) — нужна стабилизация имени поля времени и формата ISO из BE.

## Как проверить
1. В проекте нажать «Добавить задачу», создать «Тест».
2. Открыть карточку, поменять заголовок, сохранить → обновить страницу → изменения сохранились.
3. Перетащить задачу в Done/Today → убедиться, что появилось время закрытия.
4. Для задачи с `source="clawdbot"` проверить отображение бейджа.

## Риски / заметки
- Нужна договорённость о названии поля времени (`closed_at` vs `done_at`) и его timezone (желательно ISO8601 UTC).
- Drawer должен корректно закрываться при успешном сохранении и показывать валидацию при пустом title.
