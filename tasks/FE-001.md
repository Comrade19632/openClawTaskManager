# FE-001 — UI проектов + Канбан (5 колонок) + Drawer редактирования + Drag&Drop

> Примечание: этот файл — агрегатор. Основная работа ведётся по более гранулярным задачам:
> - `FE-001-projects-list-create.md`
> - `FE-002-board-ui-dnd-columns.md`
> - `FE-003-task-card-drawer-metadata.md`

## Контекст
Спринт: MVP доски TaskManager под префиксом `/taskManager/`.
Сейчас во фронтенде есть:
- логин через Telegram Login Widget
- хранение токена в `localStorage`
- заглушка «Проекты» и заглушка «Канбан» (без API, без DnD)

Нужно довести UI до MVP: реальные проекты пользователя + доска проекта + карточки задач с редактированием и переносом **строго Drag&Drop** с сохранением статуса через API.

## Scope (что делаем)
1) Экран «Проекты»
- Список проектов пользователя (из API).
- Создание проекта (минимум: `name`, опционально `description`).
- Открытие доски выбранного проекта.

2) Экран «Доска проекта»
- 5 колонок в UI (и как минимум 1:1 с бэкенд-статусами; порядок как в `SPRINT.md`):
  - Queue
  - In Progress
  - Waiting
  - Done
  - Today
- Отображение карточек задач по колонкам.
- Создание задачи (обязателен `title`) в текущем проекте (дефолтный статус: Queue).
- Открытие Drawer по клику на карточку: редактирование title/description (и опциональных полей, если добавятся).

3) Drag&Drop
- Перенос карточек **строго через drag&drop** между колонками.
- При drop — запрос к API на смену статуса (и позиции, если бэкенд поддержит ordering).
- При ошибке API — откат UI/показ ошибки.

4) Визуальные требования карточки
- Badge 🤖 если `source == "clawdbot"`.
- В правом нижнем углу показывать «время закрытия» (коротко: дата+время), **только если задача в колонке Done или Today** и поле `closed_at/done_at` пришло из API.

5) Токенная сессия (DRF TokenAuth)
- Все запросы к API (кроме login) идут с `Authorization: Token <token>`.
- При 401 — разлогин (чистим token) и возвращаем на логин.

## Acceptance Criteria (Готово)
- [ ] Пользователь после Telegram login попадает на «Проекты» и видит список проектов из API.
- [ ] Пользователь может создать проект (минимум имя) и сразу видит его в списке.
- [ ] Пользователь может открыть доску проекта и видит 5 колонок (Queue/In Progress/Waiting/Done/Today).
- [ ] Пользователь видит задачи в соответствующих колонках (данные из API).
- [ ] Создание задачи с обязательным `title` работает (валидация/ошибка отображается понятным текстом).
- [ ] Drawer редактирования открывается по клику на карточку и сохраняет изменения через API.
- [ ] Задачу можно переместить между колонками **только DnD**; после drop статус сохраняется в БД (подтверждается повторной загрузкой).
- [ ] При переносе в Done/Today отображается время закрытия (date+time) в правом нижнем углу карточки.
- [ ] Если `source=="clawdbot"`, отображается badge 🤖.
- [ ] Приложение стабильно работает под `/taskManager/` (все ссылки/запросы используют base path).
- [ ] Основные пользовательские тексты/лейблы в UI — на русском (как в `SPRINT.md`).

## Как проверить (ручной чеклист)
1) Открыть `/taskManager/`.
2) Залогиниться через Telegram widget → получить token.
3) На «Проекты»:
   - запрос `GET /taskManager/api/projects/` возвращает список;
   - создать проект через UI → уходит `POST /projects/` → появляется в списке.
4) Открыть проект:
   - `GET /projects/:id/board` (или эквивалент) грузит задачи;
   - есть 5 колонок.
5) Создать задачу → `POST /tasks/` → карточка появляется в Queue.
6) Открыть Drawer → изменить title → `PATCH /tasks/:id/` → изменения видны после refresh.
7) Перетащить карточку в In Progress/Waiting/Today/Done → уходит запрос смены статуса → после refresh задача в новой колонке.
8) Перетащить в Done/Today → на карточке появился `closed_at` (внизу справа).
9) Подставить задачу с `source="clawdbot"` (через фикстуру/админку/ручной запрос) → отображается 🤖.

## Риски / заметки
- Требование «DnD строго обязателен» → не делаем fallback-кнопки «переместить», иначе не соответствует SPRINT.
- Нужно синхронизировать названия/enum статусов с бэкендом (лучше договориться о константах/маппинге).
- Возможны edge cases: быстрые последовательные drag, конфликт optimistic UI vs сервер.
- Сейчас весь UI лежит в одном файле `src/main.jsx` → высокий риск сложности поддержки/мердж-конфликтов (см. TECH_DEBT).
