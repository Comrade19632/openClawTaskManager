# OpenClaw TaskManager

Trello‑подобный Kanban (интерфейс полностью на русском) для трекинга проектов и задач.

## Доступ
- UI: `/taskManager/`
- API: `/taskManager/api/`

## Стек
- Backend: Django + DRF
- Frontend: React + Vite
- DB: Postgres
- Infra: Docker Compose + Nginx (reverse proxy)

## Полное ТЗ (PRD)

### 1) Авторизация
**Прод:** логин через **Telegram Login Widget** (не WebApp initData).
- Пользователь нажимает «Login with Telegram»
- Фронт отправляет payload виджета на backend
- Backend валидирует подпись по документации Telegram Login Widget
- Backend выдаёт токен (DRF TokenAuth)

**Dev-режим:** допускается dev‑логин для тестового пользователя, но:
- только с `localhost/127.0.0.1`
- только при `DJANGO_DEBUG=1`

### 2) Модель продукта
#### Проекты
- Список проектов (каждый проект = доска)
- Настройки проекта (иконка шестерёнки)
  - пока минимум: «ТЗ проекта» (текстовое поле)

#### Канбан
Колонки:
- Queue
- In Progress
- Waiting
- Done
- Today (это Done с `done_at = today`)

Требования:
- Drag&Drop между колонками
- Анимация перемещения
- Optimistic UI (мгновенно в UI, потом подтверждение от API)
- Счётчики в шапках колонок

#### Карточки задач
Поля:
- `title` (обязательное)
- `description` (опционально)
- `status` (колонка)
- `created_at`, `updated_at`
- badge 🤖 если `source == "clawdbot"`

UI:
- Добавление задачи (title/description/status)
- Drawer (выезжающая панель) для редактирования

### 3) Realtime
- Реалтайм обновления из Postgres через LISTEN/NOTIFY
- Доставка на фронт через SSE (или WebSocket — если решим)

### 4) Нефункциональные требования
- Всё на русском (UI/тексты/сообщения)
- Секреты не коммитить (`.env` в `.gitignore`)

## Файлы управления проектом (source of truth)
В корне репозитория:
- `STATUS.md` — текущий прогресс, блокеры, что дальше
- `SPRINT.md` — задачи текущего спринта (бизнес-уровень) + DoD
- `BACKLOG.md` — будущие задачи
- `TECH_DEBT.md` — техдолг по итогам code review
- `tasks/*.md` — технические задачи по ролям (создаёт TeamLead)

## Локальный запуск (Docker)
```bash
cd /root/.openclaw/workspace/projects/openClawTaskManager
docker compose up -d --build
```

Порты (на VPS):
- backend: `127.0.0.1:8010`
- frontend dev: `127.0.0.1:5180`

Проверки:
- `curl https://<domain>/taskManager/api/health/`

## API (первичное)
- `GET /taskManager/api/health/`
- `POST /taskManager/api/auth/telegram/` — логин через Telegram Login Widget
- `POST /taskManager/api/auth/dev/` — dev-only (localhost + DEBUG)
- `GET /taskManager/api/me/` — текущий пользователь (TokenAuth)

